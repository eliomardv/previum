POSTGRES_DB=previum
POSTGRES_USER=previum_user
POSTGRES_PASSWORD=
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_PUBLISHED_PORT=5434
# Na execução local, DATABASE_URL prevalece sobre POSTGRES_*.
# Compose usa este arquivo da raiz e passa os campos separados à API.
# Para API local, exporte as variáveis ou use backend/.env:
# POSTGRES_HOST=localhost e POSTGRES_PORT=5434.
# Gere com: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=
ACCESS_TOKEN_EXPIRE_MINUTES=1500
API_PUBLISHED_PORT=8001
